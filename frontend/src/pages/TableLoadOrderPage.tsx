import { Box, Typography } from '@mui/material';

const TableLoadOrderPage = () => (
  <Box>
    <Typography variant="h4" gutterBottom>
      Table Load Order
    </Typography>
    <Typography color="text.secondary">Sequence controls for table loads will be configured here.</Typography>
  </Box>
);

export default TableLoadOrderPage;
