import { Box, Typography } from '@mui/material';

const ConstructedTablePage = () => (
  <Box>
    <Typography variant="h4" gutterBottom>
      Constructed Tables
    </Typography>
    <Typography color="text.secondary">Review constructed tables and their pipelines.</Typography>
  </Box>
);

export default ConstructedTablePage;
