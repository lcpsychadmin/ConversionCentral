import { Box, Typography } from '@mui/material';

const ApprovalsPage = () => (
  <Box>
    <Typography variant="h4" gutterBottom>
      Approval Center
    </Typography>
    <Typography color="text.secondary">Manage pending approvals from this view.</Typography>
  </Box>
);

export default ApprovalsPage;
